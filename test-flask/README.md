# Test-Flask

Advanced Flask starter with Flask-Security-Too.

Sample accounts:
- `user@example.com` / `password` (role: user)
- `admin@example.com` / `password` (role: admin)

Run `python run.py` after installing requirements.
